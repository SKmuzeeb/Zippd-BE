DATABASE_URL=            # Neon connection string, e.g. postgresql://user:pass@ep-xxx.neon.tech/dbname?sslmode=require
JWT_SECRET=               # random 32+ char string
ALLOWED_ORIGIN=           # e.g. https://zippd.vercel.app
PORT=4000
NODE_ENV=development
EMAIL_API_KEY=            # optional — magic-link email sending, blank = dev mode logs the link instead
